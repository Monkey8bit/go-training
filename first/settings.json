{
    "name": "Go: Launch file with redirection",
    "type": "go",
    "request": "launch",
    "mode": "debug",
    "program": "${file}",
    "cwd": "${fileDirname}",
    "debugAdapter": "legacy",
    "dlvFlags": [
        "-r",
        "stdin:${fileDirname}/tests/1"
    ]
}